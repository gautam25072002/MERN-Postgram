<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My app</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        
        <div class="flex justify-between items-center mt-2">
            <div class="flex gap-2 overflow-hidden">
                <div class="w-10 rounded-full h-10">
                    <a href="/profile/upload">
                        <img class="w-full h-full rounded-full object-cover " src="/images/uploads/<%= user.profilepic%>" alt="profile pic">
                    </a>
                </div>
                <h3 class="text-3xl mb-2">
                    <% if(user._id.toLocaleString() === currentUser._id.toLocaleString()){ %>
                        <span class="font-light">Hello</span>, <%= user.name%>👋
                    <% } else{%>
                        <span><%=user.username%></span>
                    <%}%>
                </h3>
            </div>
            <div class="flex justify-center items-center gap-6">
                <a href="/feed" class="border-2 px-3 hover:scale-95 py-1 text-md hover:border-pink-500 ml-4">Home</a>
                <a href="/logout" class="border-2 px-3 hover:scale-95 py-1 text-md hover:border-red-600">Logout</a>
            </div>
        </div>
        <h5 class="mb-3 text-sm">You can create a new post.</h5>
        <form action="/post" method="post" class="mt-4">
            <textarea placeholder="What's on your mind Today?" name="content" class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3 block"></textarea>
            <input type="submit" value="Create Post" class="px-3 text-sm py-2 bg-pink-500 w-40 cursor-pointer hover:scale-95 hover:bg-pink-600 rounded-md mt-3">
        </form>

        <div class="posts mt-20"
        <h3 class="text-zinc-400"> 
            <% if(user._id.toLocaleString() === currentUser._id.toLocaleString()){ %>
                Your Posts
            <% } else { %>
                <%= user.username %>'s Posts
            <% } %>
        </h3>
        <% if(user.posts.length > 0){ %>
                <div class="post-container mt-5 flex flex-wrap gap-3">
                    <% user.posts.reverse().forEach((post) => { %>
                        <div class="w-[350px] p-4 mt-2 border-2 h-54 border-zinc-600 rounded-md bg-zinc-800">
                            <h4 class="text-sm text-blue-500 mb-2">@<%= user.username%></h4>
                            <div class="flex justify-between">
                                <p class="text-md tracking-tight max-w-[70%]"><%= post.content%></p>
                                <p class="text-sm text-zinc-500 mt-[4px]"><%= new Date(post.date).toLocaleString('en-IN',{dateStyle: 'medium', timestyle: 'short'})%></p>
                            </div>
                            <small class="mt-2 inline-block"><%= post.likes.length%>🩷</small>
                            <div class="btns gap-4 flex mt-6">
                                <a href="/like/<%= post._id%>" class="text-pink-500">
                                    <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike"%>
                                </a>
                                <div class="flex gap-4">
                                    <% if(user._id.toLocaleString() === currentUser._id.toLocaleString()){ %>
                                    <a href="/edit/<%= post._id%>" class="text-zinc-500">
                                       <i class="fa-solid fa-pen"></i>
                                    </a>
                                    <form action="/delete/<%= post._id%>" method="post" >
                                        <button class="text-red-600 cursor-pointer" onclick="return confirm('Are you sure you want to delete this post?')">
                                            <i class="fa-solid fa-trash"></i>
                                        </button>
                                    </form>
                                    <% } %>
                                </div>
                                
                            </div>
                        </div>
                      <%})%>
                     <% } else { %>
                   <h3 class="mt-4 p-3 text-zinc-500">No posts yet.</h3>
                 <% } %>
                </div>
        </div>
    </div>
</body>
</html>