<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My app</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        
        <div class="flex justify-between items-center mb-6">
            <a href="/profile" class="text-md hover:text-blue-500">Go Back to Profile</a>
            <a href="/logout" class="border-2 px-2 py-1 text-md hover:border-red-600">Logout</a>
        </div>
        <h2 class="mb-5 text-2xl">Edit Post</h2>
        <form action="/edit/<%= post._id%>" method="post">
            <textarea placeholder="What's on your mind Today?" name="content" class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md p-3 block"><%= post.content%></textarea>
            <input type="submit" value="edit post" class="px-3 text-sm py-2 bg-yellow-600 w-40 rounded-md mt-3">
        </form>

        
    </div>
</body>
</html>